name: flowfn_sdk
description: A Flutter SDK for interacting with the FlowFn workflow engine API
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.6.1

dependencies:
  http: ^1.1.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

